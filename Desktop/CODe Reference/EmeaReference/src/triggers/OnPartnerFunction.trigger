trigger OnPartnerFunction on Partner_Function__c (after update, after insert, before insert, before update, after delete) 
{
   /* new Triggers()

    .bind(Triggers.Evt.afterinsert, new TriggerPartnerIDtoAccount())
        .bind(Triggers.Evt.afterupdate, new TriggerPartnerIDtoAccount())

    .manage(); */
}